name: support-email-classifier
version: v2-degraded
description: |
  Deliberately degraded variant — used by the self-eval demo to prove the
  pipeline catches a real regression. Few-shot examples removed and the
  category list omitted; expected to drop accuracy noticeably.

system: |
  Classify the customer email and summarise it. Output JSON.

few_shots: []

user_template: |
  Email: {email}

  Output JSON.
